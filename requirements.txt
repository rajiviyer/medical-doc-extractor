# Core dependencies for document processing
PyPDF2
pdfplumber
pytesseract
pdf2image
Pillow
python-dotenv
openai
mistralai
# langchain_mistralai

# For NER/LLM integration, uncomment as needed:
# spacy
# transformers
# openai

# System dependencies (install via apt, not pip):
# - Tesseract OCR: sudo apt install tesseract-ocr
# - Poppler for pdf2image: sudo apt install poppler-utils 